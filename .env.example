# ===========================================
# ResumeAI Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values
# Never commit .env file to version control!

# ===========================================
# API Keys
# ===========================================
# Get your Gemini API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# Model Configuration
# ===========================================
# Gemini model to use for parsing
GEMINI_MODEL=gemini-2.5-flash-lite
# Alternatives:
#   gemini-2.5-flash      - More accurate but 3x slower (33s per resume)
#   gemini-2.5-pro        - Most accurate but 10x slower
# Recommended: gemini-2.5-flash-lite for speed (9s per resume)

# Embedding model for semantic similarity
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# Alternative: all-mpnet-base-v2 (more accurate but larger)

# ===========================================
# Scoring Weights (sum must equal 1.0)
# ===========================================
WEIGHT_SKILLS=0.30
WEIGHT_EXPERIENCE=0.25
WEIGHT_SEMANTIC=0.25
WEIGHT_EDUCATION=0.15
WEIGHT_LOCATION=0.05

